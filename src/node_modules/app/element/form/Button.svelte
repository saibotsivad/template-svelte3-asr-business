<script>
	export let style = 'light'
	export let block = false
	export let size = '' // 'large' or 'small'
	export let disabled = false
</script>

<style lang="scss">
	button {
		border: 1px solid transparent;
		font-size: 1rem;
		padding: 0.5rem 1.5rem;
		border-radius: 0.2rem;
		cursor: pointer;
		color: var(--color-light);
		background-color: var(--bgc)
		&:hover {
			background-color: red; /*var(--hovc);*/
			transition: background-color 300ms ease-in;
		}
	}
	button:disabled {
		filter: opacity(0.9) hue-rotate(5deg);
	}
	button.block {
		display: inline-block;
		width: 100%;
	}
</style>

<button
	style="--bgc: var(--color-{style}); --hovc: var(--color-{style}-dark);"
	on:click
	class:block
	disabled={disabled}
>
	<slot></slot>
</button>
