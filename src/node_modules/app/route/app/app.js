import App from './App.svelte'
import dashboard from './dashboard/dashboard.js'

export default ({ asr }) => {
	asr.addState({
		name: 'app',
		route: '/app',
		defaultChild: 'dashboard',
		template: App,
		resolve: async (data, parameters) => {
			// do a fetch of all the core account data
			return {
				hello: 'world'
			}
		}
	})

	dashboard({ asr })
}
