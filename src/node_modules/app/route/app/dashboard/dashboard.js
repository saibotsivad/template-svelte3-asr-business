import Dashboard from './Dashboard.svelte'

export default ({ asr }) => {
	asr.addState({
		name: 'app.dashboard',
		route: '/dashboard',
		template: Dashboard,
		resolve: async (data, parameters) => {
			// do a fetch of minimal dashboard data
			return {
				hello: 'world'
			}
		},
		activate({ domApi }) {
			// fetch more data and update
		}
	})
}
