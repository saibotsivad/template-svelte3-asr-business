import Login from './Login.svelte'

export default ({ asr }) => {
	asr.addState({
		name: 'login',
		route: '/login',
		template: Login,
		resolve: async (data, parameters) => {
			// do a fetch of the current user
			// if logged in, redirect to wherever they were going
			return {
				name: 'world'
			}
		}
	})
}
