import PasswordReset from './PasswordReset.svelte'

export default ({ asr }) => {
	asr.addState({
		name: 'password-reset',
		route: '/password-reset',
		template: PasswordReset,
		activate: ({ domApi }) => {
			domApi.$on('TODO', async () => {
				console.log('do something')
			})
		}
	})
}
